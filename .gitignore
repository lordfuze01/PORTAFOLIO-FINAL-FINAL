node_modules
.DS_Store
.claude
*.log
Thumbs.db

# Imagenes fuente pesadas: no se suben al repo.
# El sitio usa la version optimizada (foto-portada.jpg).
FOTO PORTADA FINAL.jpg
FOTO PORTADA.jpg
DSC04623-2.jpg

# Fotos originales sin comprimir de los hover de trabajos.html (una pesa
# 9MB). El sitio usa las versiones livianas en imagenes/*-hover.jpg.
imagenes/ESPECIFERAL HOVER.png
imagenes/MORDIENDO EL POLVO HOVER.jpg
imagenes/EL SABIO SE PARCHA EL BOBO SE ESTRESA HOVER.png

# Fotos originales sin comprimir de la galeria Masonry (varias de mas de
# 5MB). El sitio usa las versiones optimizadas en imagenes/masonry/.
FOTOS MASSONERY/

# Estado local de la skill de diseno (consentimiento del hook de esta
# maquina). El sistema de diseno en si -- .impeccable/design.json -- SI
# va al repo: es artefacto del proyecto, no configuracion personal.
.impeccable/config.local.json

# Secretos / variables de entorno: NUNCA al repo.
# (Hoy no existe ningun .env; la regla queda lista para cuando haga falta.)
.env
.env.*

# Salida generada por graphify (grafo local del codigo): es derivada,
# contiene rutas absolutas de esta maquina y cambia en cada corrida.
# Se regenera con: graphify update .
graphify-out/
